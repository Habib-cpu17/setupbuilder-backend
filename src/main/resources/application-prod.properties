# ===== Production (Render) — all values from env vars =====
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# ===== CORS =====
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS}