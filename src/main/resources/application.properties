spring.application.name=setupbuilder-backend

# ===== Server =====
server.port=8080

# ===== Database =====
spring.datasource.url=postgresql://setupbuilder_db_user:eQOdFEMgjgr0uzQawU6tntO8m9DrQilU@dpg-dam19j0ae00c73cnng3g-a/setupbuilder_db
spring.datasource.username=root
spring.datasource.password=@Hammad12
spring.datasource.driver-class-name=org.postgresql.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===== Jackson =====
spring.jackson.datatype.datetime.write-dates-as-timestamps=false

# ===== Pricing =====
pricing.provider=mock
pricing.refresh.cron=0 0 3 * * *
pricing.refresh.enabled=true

# ===== Gemini AI =====
gemini.api.key=AQ.Ab8RN6JS8-i_yDz-PsM5h_3RMzOH6PKoWXOMrtHisgrUdQYASg
gemini.api.base-url=https://generativelanguage.googleapis.com/v1beta/models
gemini.api.models=gemini-flash-latest,gemini-flash-lite-latest
gemini.api.retries=2

# ===== Firebase =====
# The service account file is loaded from src/main/resources/
# In production, you would use an environment variable for the JSON content.

# ===== File Uploads =====
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB

# Where files are stored on disk (relative to app working directory)
app.upload.dir=uploads

# Per-type size limits (bytes)
app.upload.avatar-max-size=2097152
app.upload.banner-max-size=5242880

# ===== Profiles =====
# The 'local' profile is for your local machine only.
# spring.profiles.active=local